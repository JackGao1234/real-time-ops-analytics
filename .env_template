# =========================================================
# ClickHouse 連線設定 (用於 Processor 連線和 CH 容器初始化)
# =========================================================
# 確保這些變數在 ClickHouse 容器啟動時用於創建使用者
CLICKHOUSE_USER=myuser
CLICKHOUSE_PASSWORD=mypassword123
CLICKHOUSE_DB=analytic_db

# Processor 程式碼中使用的變數
CH_HOST=clickhouse # 內部服務名稱
# CH_PORT=8123 (不需要定義，因為我們已在 main.py 中硬編碼為 8123 HTTP 端口)

# =========================================================
# Kafka/Redpanda 設定 (用於 Producer 和 Processor 連線)
# =========================================================
# 外部 Broker 地址 (用於 Producer 在主機上運行時連線)
KAFKA_EXTERNAL_PORT=19092
KAFKA_BROKER_EXTERNAL=localhost:19092

# 內部 Broker 地址 (用於 Processor 在容器內連線)
KAFKA_BROKER_INTERNAL=redpanda:9092

# Topic 和 Consumer Group
ORDER_TOPIC=order_topic
PAYMENT_TOPIC=payment_topic
KAFKA_CONSUMER_GROUP=analytics_processor_group

# Processor 程式設定
POLL_TIMEOUT_MS=1000
